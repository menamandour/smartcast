import 'package:flutter/material.dart';

class Page18 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 18')),
      body: Center(child: Text('This is Page 18')),
    );
  }
}

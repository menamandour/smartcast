import 'package:flutter/material.dart';

class Page17 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 17')),
      body: Center(child: Text('This is Page 17')),
    );
  }
}

import 'package:flutter/material.dart';

class Page14 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 14')),
      body: Center(child: Text('This is Page 14')),
    );
  }
}

import 'package:flutter/material.dart';

class Page15 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 15')),
      body: Center(child: Text('This is Page 15')),
    );
  }
}

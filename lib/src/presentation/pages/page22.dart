import 'package:flutter/material.dart';

class Page22 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 22')),
      body: Center(child: Text('This is Page 22')),
    );
  }
}

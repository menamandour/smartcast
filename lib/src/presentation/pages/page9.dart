import 'package:flutter/material.dart';

class Page9 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 9')),
      body: Center(child: Text('This is Page 9')),
    );
  }
}

import 'package:flutter/material.dart';

class Page11 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 11')),
      body: Center(child: Text('This is Page 11')),
    );
  }
}

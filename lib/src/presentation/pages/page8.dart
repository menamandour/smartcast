import 'package:flutter/material.dart';

class Page8 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 8')),
      body: Center(child: Text('This is Page 8')),
    );
  }
}

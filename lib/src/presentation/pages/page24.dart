import 'package:flutter/material.dart';

class Page24 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 24')),
      body: Center(child: Text('This is Page 24')),
    );
  }
}

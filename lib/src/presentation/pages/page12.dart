import 'package:flutter/material.dart';

class Page12 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 12')),
      body: Center(child: Text('This is Page 12')),
    );
  }
}

import 'package:flutter/material.dart';

class Page20 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 20')),
      body: Center(child: Text('This is Page 20')),
    );
  }
}

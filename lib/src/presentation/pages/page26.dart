import 'package:flutter/material.dart';

class Page26 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 26')),
      body: Center(child: Text('This is Page 26')),
    );
  }
}

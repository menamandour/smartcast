import 'package:flutter/material.dart';

class Page10 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 10')),
      body: Center(child: Text('This is Page 10')),
    );
  }
}

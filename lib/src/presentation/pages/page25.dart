import 'package:flutter/material.dart';

class Page25 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 25')),
      body: Center(child: Text('This is Page 25')),
    );
  }
}

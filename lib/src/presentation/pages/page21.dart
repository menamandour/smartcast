import 'package:flutter/material.dart';

class Page21 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page 21')),
      body: Center(child: Text('This is Page 21')),
    );
  }
}
